# v0.11.31 HYBRID Release Audit

- 基线：v0.11.30 HYBRID；双时间轴、source 顺序、当前状态解析、0 API、30 条批次、自动起点、524 保底和地点保护均保留。
- manifest version 与两个可见徽标：0.11.31。
- 具体钟点证据：同 source canonical 正文或允许的同楼状态元数据必须能够验证 timeline 中的全部具体钟点。
- 摘要隔离：`abstract/story_summary` 等生成式摘要块从钟点证据文本中移除，避免模型生成的 15:00 自我验证。
- 精度降级：无法验证的 `下午3:00` 转为 `下午（具体时刻未明确）`；无法形成单一自然时段时转为“时间未明确”。
- 中文钟制：`下午3:00` 按 15:00 处理；原文明示的 `下午3:30—5:00` 两端均可验证并保留。
- 锚点安全：降级后的时段不产生 minute clock，不参与倒退、跨午夜及钟点排序判断。
- 双轴安全：场景日期与 MVU 现实日期不同时，现实元数据不能验证场景 timeline 的精确钟点。
- 无摘要迁移：旧楼层即使没有任何预设摘要，也会使用已有 source 正文复核；无证据分钟本地降级。
- 边界归属：截图形态的 `#0 2026 开场 → #11/#13 无摘要 → #15 明确 2003 场景` 回归中，#11/#13 按 source 邻近性归入 2003 场景，并只保留低精度时段。
- 事件完整性：日期/时间回归中 event 文本和 source 顺序逐项保持不变。
- 当前状态：最新明确“即时状态 2003-10-28 13:45”仍优先于较早摘要；current reality clock 单独保留。
- 默认总结方式：`summaryMode=local`；普通增量和阶段总结测试中模型调用数为 0。
- 524 安全：AI 模式第一次 Cloudflare 524 后立即本地提交并切换 local。
- 原聊天 JSONL：无写入、删除、清空或全量重建路径新增。
- JS syntax、manifest JSON、v0.11.27 旧回归及 v0.11.31 时间精度/场景日历/0 API/524 回归全部通过。
- ZIP：打包后解压复验文件清单、manifest/徽标、JS syntax 和相关回归。
