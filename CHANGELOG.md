# Story Memory Manager v0.11.7

## 0 API 时间轴一致性修复

- 将 0 API 补档的日期、星期和时分改为统一校准，不再分别处理。
- 同 source 的 `/世界/当前日期`、`/世界/当前时间`、`/世界/当前地点` 继续只作为结构化元数据读取，不进入剧情事实正文。
- 修复「时间已经是周一/周二，但 timeline 仍挂在前一天日期组」的问题。
- 当日期可靠而星期错误时，以绝对日期为准并修正星期显示。
- 当日期只是继承值、星期明确指向下一天时，只允许向前推进 1 天，不允许向后倒退。
- 保留深夜→凌晨的跨日校准；弱来源的同日倒退钟点会被拒绝。
- 时间线修复按钮升级为「校准时间线日期/星期/时分（0 API）」，会处理：未明确时间、错误日期分组、星期冲突、同日倒退。

## 0 API 摘要清理

- 新增对 `serial:... time:... scene:... plot:...` 风格 `<abstract>` 的结构化解析。
- `serial / time / NSFW / scene / plot` 等预设元数据不会再进入事件正文。
- 改进本地压缩后的重复人名、残余标点、短残句清理。
- 当抽取结果只剩「歪头/看着/咬嘴唇」等弱动作时，会优先保留一个非露骨、与决定/询问相关的短对白事实（若存在），避免形成无意义 timeline。

## 保持不变

- 代码补档 / 重建仍为 0 API。
- 不修改原聊天 JSONL。
- 不改变 `last_processed_index`。
- 不用本地代码猜人物关系、主线、待办或人物锚点。
- 断档保护、失败回滚、UpdateVariable 白名单隔离继续保留。
