# Story Memory Manager v0.11.5

## 0 API 结构化补档重建

- 修复 v0.11.4 把 `<abstract>` 中多条编号记录整块拼进一条 timeline 的问题。
- 新增结构化摘要解析：识别类似 `049 2025-09-23 | 12:55-13:00 地点 事件` 的记录，并拆成独立 timeline。
- 摘要编号（如 `049/050`）、日期、时间从 event 正文中剥离；日期/时间写入对应字段。
- 在能可靠识别时，把地点前缀拆到 `location` 字段；无法可靠识别时保留在 event，避免误切。
- 结构化摘要中的时间标记为“预设摘要时间”，不再误标为“原文明确时间”或统一套用本轮结束时间。
- 结构化记录不再被“两对消息合并”逻辑拼成大块；只有无结构化摘要的 fallback 节点仍允许有限合并。
- 对相邻楼层重复携带的同一结构化摘要做去重，并合并 source 覆盖。
- 若该范围已经执行过 v0.11.4/v0.11.5 代码补档，再次运行会先移除完全位于目标范围内的旧代码补档 timeline，再按 v0.11.5 结构化规则重建。
- 当前已无断档但存在 v0.11.4 代码补档审计时，历史重建界面会自动回填上一次补档范围，方便直接重建。
- 整个流程仍为 0 API，不调用任何总结模型。

## 保留的安全机制

- 原聊天不修改。
- `last_processed_index` 不改变。
- 人物、关系、facts、待办、主线、当前场景等不由本地代码重写。
- 继续使用 `/世界/当前日期`、`/世界/当前时间`、`/世界/当前地点` 白名单元数据。
- 失败自动恢复补档前 memory。
- v0.11.2/v0.11.3 的 silent-drop 游标保护、source 校验、断档检测、断档期间禁止安全注入/自动隐藏继续保留。
