# Story Memory Manager v0.11.6

## 0 API 事实压缩

- 修复 v0.11.5 fallback timeline 仍可能复制大段对白、感叹和正文细节的问题。
- 新增确定性本地事实压缩：优先保留到达、离开、受伤、救援、检查、治疗、决定、警告、联系等剧情动作。
- 过滤大段直接对白、重复感叹与过细的身体/动作微描写。
- 对角色名锚定后的第一/第二人称摘要做有限视角归一化，降低“我/你”直接进入长期记忆的概率。
- event 目标长度约 60–190 字；不为了压缩而创造未出现的桥接事实。

## 0 API 旧时间线修复

- 修复“目标缺口已经重建，但 #1623+ 等既有 timeline 仍显示未明确时间”的问题。
- `代码压缩重建这个范围（0 API）` 完成后，会继续扫描到当前 `last_processed_index`，只填补 unresolved date/time/location。
- 新增独立按钮：`仅修复时间线“未明确时间”（0 API）`。
- 时间来源优先级：同 source `/世界/当前时间` → 同 source 结构化 `<abstract>` 时间 → 同 source 原文明确信息。
- 已经存在且可验证的明确时间绝不覆盖。
- 修复后的 `/世界/当前时间` 标记为“变量状态时间”，结构化摘要时间标记为“预设摘要时间”。

## 保留安全机制

- 0 API，不调用总结模型。
- 原聊天不修改。
- `last_processed_index` 不改变。
- 人物、关系、facts、待办、主线、当前场景不由本地代码重写。
- UpdateVariable 仍只白名单读取 `/世界/当前日期`、`/世界/当前时间`、`/世界/当前地点`。
- 好感度、gossip、Analysis、其他 JSONPatch 不进入 canonical timeline。
- 失败恢复操作前 memory。
- v0.11.2/v0.11.3 的 silent-drop、source 校验、断档保护继续保留。
