# Story Memory Manager v0.6.0

修复 v0.5.9 的关键逻辑错误：

旧版 `explicitDateFromEvent()` 把记忆对象自身的 `date` 字段也当成了“原始聊天明确日期”。
因此旧记忆里所有 2025-09-10 都被锁死，跨午夜状态机根本没有机会推进日期，
表现就是“跨午夜 0｜日期变更 0”。

v0.6.0 改为：
- `date` 只是旧记忆归属，不是证据。
- 只有 `time/event` 原文真的写出绝对日期时才锁定日期。
- source 顺序晚间/深夜 → 凌晨/半夜时日期 +1。
- 凌晨之后到当日晚间不再次 +1。
